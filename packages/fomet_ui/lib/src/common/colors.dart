import 'dart:ui';

/// Colors from the Fomet UI component library.
abstract final class FometColors {
  /// Seed color for the theme.
  static const seed = Color(0xFFFFFFFF);

  /// Primary color.
  static const primary = Color(0xFF95C01E);

  /// Secondary color.
  static const secondary = Color(0xFFD21120);

  /// Primary hovered color.
  static const primaryHovered = Color(0xFFB6ED37);

  /// Secondary hovered color.
  static const secondaryHovered = Color(0xFFF04A57);

  /// Primary text color.
  static const primaryText = Color(0xFF000000);

  /// Secondary text color.
  static const secondaryText = Color(0xFF633B1A);
}
